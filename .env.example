# Copy this file to .env and adjust values as needed.
# The included .env already has working defaults for local development.

PORT=5000
JWT_SECRET=change_this_to_a_long_random_string_in_production
NODE_ENV=development
